# WEB050-AUTHRESTORE001

Retour strict de `web/app.js` et `web/index.html` à l'état WEB050 connu comme
fonctionnel pour l'authentification Google, avec simple cache-bust.

Objectif : restaurer immédiatement la connexion avant toute nouvelle évolution UI.
